{
  "name": "predicte-commit",
  "displayName": "Predicte AI Commit Message Generator",
  "description": "Automatically generate git commit messages using AI for FREE following conventional commits guidelines. Powered by Mistral AI or Local LLMs (Ollama, vLLM, LMstudio)",
  "version": "1.0.2",
  "publisher": "predicte",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/gasatrya/predicte-commit.git"
  },
  "bugs": {
    "url": "https://github.com/gasatrya/predicte-commit/issues"
  },
  "homepage": "https://github.com/gasatrya/predicte-commit#readme",
  "icon": "images/icon.png",
  "engines": {
    "vscode": "^1.108.0"
  },
  "categories": [
    "AI",
    "Programming Languages",
    "Machine Learning",
    "Other"
  ],
  "keywords": [
    "git",
    "commit",
    "message",
    "mistral",
    "ollama",
    "devstral",
    "ai",
    "conventional-commits",
    "vllm",
    "lmstudio"
  ],
  "activationEvents": [
    "onStartupFinished"
  ],
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "predicteCommit.generateMessage",
        "title": "Predicte Commit: Generate Message",
        "icon": "$(sparkle)"
      },
      {
        "command": "predicteCommit.setApiKey",
        "title": "Predicte Commit: Set API Key"
      },
      {
        "command": "predicteCommit.setOpenAiCompatibleApiKey",
        "title": "Predicte Commit: Set OpenAI Compatible API_KEY"
      },
      {
        "command": "predicteCommit.setOpenAiCompatibleBaseUrl",
        "title": "Predicte Commit: Set OpenAI Compatible API_BASE_URL"
      }
    ],
    "menus": {
      "commandPalette": [
        {
          "command": "predicteCommit.generateMessage"
        },
        {
          "command": "predicteCommit.setApiKey"
        },
        {
          "command": "predicteCommit.setOpenAiCompatibleApiKey"
        },
        {
          "command": "predicteCommit.setOpenAiCompatibleBaseUrl"
        }
      ],
      "scm/title": [
        {
          "command": "predicteCommit.generateMessage",
          "group": "navigation"
        }
      ]
    },
    "configuration": {
      "title": "Predicte Commit",
      "properties": {
        "predicteCommit.mode": {
          "type": "string",
          "default": "remote",
          "enum": [
            "remote",
            "local"
          ],
          "enumDescriptions": [
            "Use a remote provider (Mistral AI or OpenAI-compatible API)",
            "Use a local provider (Ollama, vLLM, LM Studio)"
          ],
          "description": "Select mode: remote or local provider.",
          "order": 1
        },
        "predicteCommit.remote.provider": {
          "type": "string",
          "default": "mistral",
          "enum": [
            "mistral",
            "openai-compatible"
          ],
          "enumDescriptions": [
            "Mistral AI (default)",
            "Any OpenAI-compatible API (OpenAI, DeepSeek, Together, etc.)"
          ],
          "description": "Remote provider to use.",
          "order": 2
        },
        "predicteCommit.remote.models": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "description": "Fallback model list for the remote provider. Models are tried in order; if one fails, the next is used. If empty, provider defaults are used.",
          "order": 3
        },
        "predicteCommit.remote.baseUrl": {
          "type": "string",
          "default": "",
          "description": "Base URL for OpenAI-compatible remote provider (e.g. https://api.openai.com/v1).",
          "order": 4
        },
        "predicteCommit.remote.model": {
          "type": "string",
          "default": "",
          "description": "Single model for OpenAI-compatible remote provider. Used if remote.models is empty.",
          "order": 5
        },
        "predicteCommit.local.provider": {
          "type": "string",
          "default": "ollama",
          "enum": [
            "ollama",
            "vllm",
            "lmstudio"
          ],
          "enumDescriptions": [
            "Ollama (default)",
            "vLLM",
            "LM Studio"
          ],
          "description": "Local provider to use.",
          "order": 10
        },
        "predicteCommit.local.baseUrl": {
          "type": "string",
          "default": "",
          "description": "Base URL for local provider. Leave empty to use provider default (Ollama: :11434, vLLM: :8000, LM Studio: :1234).",
          "order": 11
        },
        "predicteCommit.local.model": {
          "type": "string",
          "default": "",
          "description": "Model name for local provider. Leave empty to use provider default.",
          "order": 12
        },
        "predicteCommit.systemPrompt": {
          "type": "string",
          "default": "You are an expert developer.\n\nTask: Write a concise git commit message for the provided code diff.\n\nRules:\n1. Output MUST be a single commit message.\n2. First line MUST be exactly one Conventional Commits subject line (e.g., 'feat: add user login').\n3. The subject line must be under 50 characters and summarize the ENTIRE change set.\n4. Do NOT output multiple subject lines (no multiple 'type:' headers).\n5. If needed, add a blank line and then a bulleted body using '- ' bullets.\n6. Do NOT output markdown code blocks or fences. Output ONLY raw commit message text.\n7. Choose the commit type based on the overall change set. Use 'docs:' only when ALL changes are documentation-only; if the changes include code/config/deps, prefer 'chore:' or 'refactor:' as appropriate.\n8. The subject should summarize the whole set and mention the most significant non-doc change if present.\n9. If the diff is empty or trivial, reply with a short error message.",
          "editPresentation": "multilineText",
          "markdownDescription": "System prompt used to generate commit messages. You can customize it in VS Code settings.",
          "order": 20
        },
        "predicteCommit.ignoredFiles": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [
            "*-lock.json",
            "*.svg",
            "dist/**"
          ],
          "description": "Glob patterns (repo-relative) to exclude from staged diff context",
          "order": 21
        },
        "predicteCommit.proxy.url": {
          "type": "string",
          "default": "",
          "description": "Proxy URL for extension HTTP requests (e.g. http://proxy.example.com:8080). Leave empty to use system proxy or no proxy.",
          "order": 30
        },
        "predicteCommit.proxy.noProxy": {
          "type": "array",
          "items": {
            "type": "string"
          },
          "default": [],
          "description": "Hosts to bypass proxy (e.g. ['localhost', '127.0.0.1', '*.internal']). Supports wildcards.",
          "order": 31
        },
        "predicteCommit.debugLogging": {
          "type": "boolean",
          "default": false,
          "description": "Log prompt payload and ignore-filter diagnostics to the Predicte Commit output channel",
          "order": 99
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run package",
    "compile": "npm run check-types && npm run lint && node esbuild.js",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "node ./node_modules/typescript/bin/tsc --noEmit --watch --project tsconfig.json",
    "package": "npm run check-types && npm run lint && node esbuild.js --production",
    "package:local": "node package-local-vsix.js",
    "compile-tests": "node ./node_modules/typescript/bin/tsc -p . --outDir out",
    "watch-tests": "node ./node_modules/typescript/bin/tsc -p . -w --outDir out",
    "pretest": "npm run compile-tests && npm run compile && npm run lint",
    "check-types": "node ./node_modules/typescript/bin/tsc --noEmit",
    "lint": "node ./node_modules/eslint/bin/eslint.js src",
    "format": "prettier --write .",
    "test": "npm run test:unit && npm run test:integration",
    "test:unit": "npm run compile-tests && npx mocha out/test/unit/**/*.test.js --ui tdd",
    "test:integration": "node ./node_modules/@vscode/test-cli/out/bin.mjs",
    "prepare": "husky"
  },
  "lint-staged": {
    "*.ts": [
      "eslint --fix",
      "prettier --write",
      "bash -c 'npm run test:unit'"
    ]
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "@types/vscode": "^1.108.0",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2",
    "@vscode/vsce": "^3.9.2",
    "esbuild": "^0.27.2",
    "eslint": "^9.39.2",
    "eslint-config-prettier": "^10.1.8",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "mocha": "^11.3.0",
    "npm-run-all": "^4.1.5",
    "prettier": "^3.8.0",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.52.0"
  },
  "dependencies": {
    "minimatch": "^10.0.3"
  },
  "overrides": {
    "diff": "^9.0.0",
    "serialize-javascript": "^7.0.6",
    "undici": "^6.27.0"
  }
}
